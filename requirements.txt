# Runtime dependencies added on top of the base Video-3D-LLM stack
# (the base stack is declared in pyproject.toml).
#
# These are the deps introduced by the Exp 3 / Exp 4 experiments.

# Voxel clustering (DBSCAN) used by llava/exp4.py
scikit-learn>=1.3

# Optional: sentence-embedding eval metric for Scan2Cap
# (cosine similarity + length-penalty scorer in llava/eval/)
sentence-transformers>=2.7

# Optional: PyYAML for run_from_config.py
PyYAML>=6.0

# ---------------------------------------------------------------------------
# nvblox_torch is NOT pip-installable. Required by llava/exp4.py (the
# voxel-cluster crop pipeline). Build instructions:
#
#   sudo apt-get install -y cmake git git-lfs build-essential python3-dev
#   git lfs install
#   git clone https://github.com/nvidia-isaac/nvblox.git
#   cd nvblox && mkdir build && cd build
#   cmake .. && make -j6
#   cd ../nvblox_torch && pip install --editable .
#
# Requires CUDA 11.4-13.2 and a GPU with compute capability >= 7.5.
# See docs at https://nvidia-isaac.github.io/nvblox/pages/installation.html
# ---------------------------------------------------------------------------
